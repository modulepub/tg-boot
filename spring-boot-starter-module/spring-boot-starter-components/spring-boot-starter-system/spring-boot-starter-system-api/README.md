
``` java
importpub.module.system.biz.util；
//获取当前登录的用户：
UserUtil.getCurrentSysUser()
```
移动端环境切换逻辑：
1、环境跟用户绑定。

2、根据用户属性（角色、来源）批量设置环境；根据用户单独设置角色

故障处理逻辑：
1、首先登录错误设置环境的后台给用户设置正确的环境地址，并标记用户是否正确环境状态为否

2、在错误环境的后台，开启强制转回正常环境，用户重新登录正确环境后，将用户是否登录正确环境标记为是

3、待正确环境的用户全部标记为正确后，代表故障已修复，可以将错误环境恢复。